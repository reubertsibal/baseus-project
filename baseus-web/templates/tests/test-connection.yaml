apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "baseus-web.fullname" . }}-test-connection"
  labels:
    {{- include "baseus-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "baseus-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
